const paragraph = document.querySelector('p');
console.log(paragraph.innerHTML)

// innerText - returns the text content of the element which is visible on the screen
const innerText = paragraph.innerText;
console.log(innerText);

// innerHTML - returns the HTML content of the element including the tags
const innerHTML = paragraph.innerHTML;
console.log(innerHTML);

// textContent - returns the text content of the element
const textContent = paragraph.textContent;
console.log(textContent);